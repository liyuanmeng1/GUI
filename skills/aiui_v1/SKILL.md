---
name: aiui_v1
description: 面向业务团队的通用企业级 Web UI 生成 Skill（开箱引导版）。首次使用时引导用户填写基础组件层、页面模板层、设计规范层三层内容，配置完成后即可生成列表页、表单页、详情页、Dashboard 等标准业务页面。触发关键词：生成UI、列表页、表单页、详情页、Dashboard、组件、UI代码、Vue页面、React页面、新建页面。
version: 1.0.0
---

# aiui_v1 — 通用企业 UI 生成 Skill

## ⚠️ 首次使用必读

> 这是一个**待配置的 UI 生成 Skill**。
>
> 在正式使用前，你需要完成三层内容的配置：
>
> - **第一层（基础层）**：声明你的前端组件库、依赖和主题配置
> - **第二层（模板层）**：提供各类页面模板（列表页/详情页/表单页/看板页）
> - **第三层（设计规范层）**：填写颜色、字体、文案、设计原则等规范
>
> **运行以下命令，立即启动开箱向导：**
>
> ```
> 帮我配置 aiui_v1
> ```
>
> 或者直接告诉 AI：**"我要开始配置 aiui_v1 skill"**

---

## 开箱状态检测（AI 执行入口）

**每次收到 UI 生成请求时，AI 必须先检查配置状态：**

```
if 01-foundation/dependencies.md 不存在 或 内容仍为占位文本:
    → 进入开箱向导（见下方「开箱引导流程」）
    → 不得直接生成代码

elif 02-templates/ 目录下无任何 .vue / .tsx 文件:
    → 提示用户先完成模板层配置
    → 引导进入「第二层：模板层配置」

else:
    → 正常执行 UI 生成工作流（见下方「工作流程」）
```

---

## 开箱引导流程

> 当检测到 skill 未完成配置时，AI 展示以下引导内容并逐步收集配置。

---

### 欢迎语（首次触发时展示）

```
👋 欢迎使用 aiui_v1 UI 生成 Skill！

在开始生成 UI 之前，我需要先了解你们团队的技术栈和设计规范。
这只需要 10-15 分钟，配置完成后就可以快速生成符合你们规范的 UI 页面。

────────────────────────────────────
📋 配置进度
────────────────────────────────────
【第一层：基础层 01-foundation】
  ☐ dependencies.md — 组件库 & 依赖声明

【第二层：模板层 02-templates】
  ☐ AppLayout — 全局框架（必须）
  ☐ ListPage — 列表页模板
  ☐ DetailPage — 详情页模板
  ☐ FormPage — 表单页模板
  ☐ DashboardPage — 看板页模板（可选）

【第三层：设计规范层 03-design-db】
  ☐ color.md — 颜色规范
  ☐ typography.md — 字体规范
  ☐ copywriting.md — 文案规范
  ☐ design-principles.md — 设计原则
────────────────────────────────────

我们从第一层开始 👇
```

---

### Step A：基础组件库

**AI 提问：**

```
━━━ 第一步：基础组件库 ━━━

请把你们前端组件库的安装地址发给我，我来直接安装。

可以是以下任意一种格式：
- npm 包名（如 antd、element-plus、@myorg/ui）
- 完整安装命令（如 npm install @myorg/ui --registry=https://your-registry.com）
- git 地址（如 https://github.com/xxx/ui-lib）
- 内网 nexus / verdaccio 地址

如果有特殊的 registry 配置或鉴权 token，也一并告诉我。
```

**收到地址后，AI 立即执行安装：**

```bash
# 根据用户提供的安装命令执行，例如：
npm install @myorg/ui --registry=https://your-registry.com
```

安装完成后追问：

```
✅ 组件库安装完成！版本：x.x.x

两个快速确认：
1. 组件的命名前缀是什么？（如 El、My、App、Ks 等，用于 import 时识别）
2. 你们产品的品牌主色是什么色值？（如 #165DFF）
```

**AI 将组件库信息整理写入 `01-foundation/dependencies.md`**，展示：

```
✅ 基础组件层配置完成！组件库已安装，规范已写入。
```

直接进入下一步。

---

### Step B：全局框架 AppLayout

```
━━━ 第三步：全局框架 AppLayout ━━━

请把你们产品全局框架的 Figma 设计稿链接发给我，并简单描述各功能区域：

📎 Figma 链接：（粘贴链接）

📝 功能区域描述（有什么填什么）：
  - 顶部导航：有什么内容？（如：Logo、一级导航标签、用户头像/下拉菜单）
  - 左侧边栏：有哪些菜单项？（菜单名称 + 层级关系）
  - 内容区：有没有特殊处理？（如：面包屑导航、页面标题区等）

没有 Figma → 只发文字描述也可以。
如果产品没有全局框架（纯单页无导航）→ 说「无框架」。
```

AI 读取 Figma 链接内容（调用 fetch_web 抓取可访问的设计稿信息）+ 结合文字描述，生成 AppLayout 代码，写入 `02-templates/AppLayout/AppLayout.{vue|tsx}`，然后展示进度并引导下一步。

---

### Step C：各类页面模板

**每个模板按同样方式收集（Figma 链接 + 功能区域描述），顺序为：ListPage → DetailPage → FormPage → DashboardPage**

**ListPage 引导：**
```
━━━ 第四步：列表页模板 ListPage ━━━

请提供列表页的设计稿信息：

📎 Figma 链接：（粘贴链接，若有）

📝 功能区域描述：
  - 页面标题区：有标题、副标题吗？有没有顶部统计信息？
  - 工具栏：有搜索框吗？有哪些筛选器？新建按钮叫什么？
  - 表格：主要展示哪些列？有操作列吗？（查看/编辑/删除）
  - 分页：有吗？默认每页几条？

没有 Figma → 只发文字描述也可以。不需要列表页 → 说「跳过」
```

**DetailPage 引导：**
```
━━━ 第五步：详情页模板 DetailPage ━━━

请提供详情页的设计稿信息：

📎 Figma 链接：（粘贴链接，若有）

📝 功能区域描述：
  - 顶部区：有面包屑导航吗？标题旁有没有状态标签（如运行中/已停止）？
  - 操作区：有哪些操作按钮？（启动/停止/编辑/删除等）
  - 内容区：有没有 Tab 切换？每个 Tab 大概展示什么内容？
  - 详情信息：用什么方式展示？（描述列表、卡片、表格等）

没有 Figma → 只发文字描述也可以。不需要 → 说「跳过」
```

**FormPage 引导：**
```
━━━ 第六步：表单页模板 FormPage ━━━

请提供表单页的设计稿信息：

📎 Figma 链接：（粘贴链接，若有）

📝 功能区域描述：
  - 布局：单列还是双列表单？
  - 分区：表单分几个区块？每个区块叫什么名字？
  - 底部：有费用预览卡片吗？确认/取消按钮在哪里？
  - 特殊表单项：有文件上传、图片选择、代码编辑器等特殊控件吗？

没有 Figma → 只发文字描述也可以。不需要 → 说「跳过」
```

**DashboardPage 引导：**
```
━━━ 第七步：看板页模板 DashboardPage（可选）━━━

请提供数据看板页的设计稿信息：

📎 Figma 链接：（粘贴链接，若有）

📝 功能区域描述：
  - 统计卡片：顶部有几个？每个展示什么指标？
  - 图表区：有哪些图表类型？（折线图/柱图/饼图等）
  - 其他区域：有排行榜、列表、详情卡片等吗？

没有 Figma → 只发文字描述也可以。不需要 → 说「跳过」
```

**收到 Figma 链接后，AI 调用 fetch_web 读取链接内容，结合功能区域描述生成模板代码，立即写入对应目录，然后展示更新后的进度。**

---

### Step D：设计规范层

**AI 统一提问（一次性询问，不分多轮）：**

```
━━━ 第八步：设计规范层 ━━━

最后一层！你们有设计规范文档吗？

有的话请上传或发送链接：
📎 设计规范文档（Figma / 飞书文档 / PDF / 图片截图 均可）

没有也完全没问题，直接说「没有」或「用默认的」，
我会自动套用一套通用的 B 端 UI 设计规范。
```

**收到内容后的处理逻辑：**

- **用户提供了设计规范文档（链接/文件/截图）**：
  - AI 读取文档内容（调用 fetch_web 或解析图片），提取颜色、字体、文案、设计原则等信息
  - 整理写入 `03-design-db/` 下对应文件
  - 用户规范中未覆盖的项，用下方默认值补全

- **用户说「没有」或「用默认的」**：
  - AI 直接用以下默认值写入所有规范文件，**无需追问**：

**默认颜色规范**（写入 `03-design-db/color.md`）：
```
品牌主色：#165DFF
成功色：#30C453，警告色：#FF7429，错误色：#F53F3F
主文字：#1D2129，次文字：#4E5969，辅助文字：#86909C
页面背景：#F5F6F8，卡片背景：#FFFFFF，边框：#E5E6E8
```

**默认字体规范**（写入 `03-design-db/typography.md`）：
```
字体栈：PingFang SC, -apple-system, BlinkMacSystemFont, Helvetica Neue, Arial, sans-serif
页面标题：20px/500，区块标题：16px/500，正文：14px/400，小字：12px/400
```

**默认文案规范**（写入 `03-design-db/copywriting.md`）：
```
语气：严肃专业（B 端）
按钮：新建 / 确认 / 取消 / 删除 / 编辑 / 查看
空值：显示 –（破折号），颜色 #BBBDBF
操作反馈：已创建 xxx / 已保存修改 / 已删除 xxx
```

**默认设计原则**（写入 `03-design-db/design-principles.md`）：
```
1. 一致性：同类组件统一样式，不自造轮子
2. 克制性：每页只呈现当前任务必需信息
3. 反馈性：异步操作必须有加载态；成功/失败必须有 Toast
4. 效率性：主操作放工具栏左上角；危险操作用 Danger 样式
5. 容错性：删除必须 Modal 二次确认，操作列必须固定在右侧
```

---

### 配置完成

所有内容写入后，更新 SKILL.md 的 description 字段，并展示：

```
🎉 aiui_v1 配置完成！

────────────────────────────────────
📋 最终配置状态
────────────────────────────────────
【第一层：基础层】
  ✅ dependencies.md

【第二层：模板层】
  ✅ AppLayout
  ✅ ListPage（或 ☑️ 跳过）
  ✅ DetailPage（或 ☑️ 跳过）
  ✅ FormPage（或 ☑️ 跳过）
  ✅ DashboardPage（或 ☑️ 跳过）

【第三层：设计规范层】
  ✅ color.md
  ✅ typography.md
  ✅ copywriting.md
  ✅ design-principles.md
────────────────────────────────────

⚠️ 生效提示：等待约 30 秒，或重启 VS Code 立即生效。

使用方法：直接跟 AI 说「帮我生成一个列表页」即可开始！
```

---

## 工作流程（配置完成后，收到 PRD 时执行）

```
Step 0  读取全局框架文件，确认 AppLayout 结构
   ↓
Step 1  分析 PRD → 列出页面清单 + 追问缺失信息
   ↓
Step 2  并行 read_file 读取：
           ① 涉及页面的所有模板文件（02-templates/）
           ② 03-design-db/ 中相关规范文件
   ↓       ← 未完成此步，不得执行任何代码生成
Step 3  制定逐页改造计划（保留 / 修改 / 新增 / 删除）
   ↓
Step 4  以模板为基础生成代码
   ↓
Step 5  对照自检清单逐条核查后才算交付完成
```

### Step 1：分析 PRD

提取：
1. 涉及哪几个页面？对应哪种模板？
2. 页面间跳转关系
3. 每个页面的核心字段、操作、状态

信息不足时，**主动追问以下项**（一次性追问，不分多轮）：

| 维度 | 追问问题 |
|------|---------|
| 操作类型 | 是新建还是编辑？还是都有？ |
| 分页 | 列表是否需要分页？默认每页几条？ |
| 状态流转 | 资源有哪些状态？哪些状态可以操作？ |
| 批量操作 | 是否需要批量操作？ |
| 数据来源 | 下拉选项是静态枚举还是接口动态获取？ |
| 空状态 | 首次使用是否有引导？ |

### Step 2：读取模板（强制，不可跳过）

根据 Step 1 的页面清单，**立即并行 read_file** 读取：

| 页面类型 | 读取路径 |
|---------|---------|
| 全局框架 | `{skill_dir}/02-templates/AppLayout/` |
| 列表页 | `{skill_dir}/02-templates/ListPage/` |
| 详情页 | `{skill_dir}/02-templates/DetailPage/` |
| 表单页 | `{skill_dir}/02-templates/FormPage/` |
| 看板页 | `{skill_dir}/02-templates/DashboardPage/` |
| 组件规范 | `{skill_dir}/03-design-db/component-spec.md`（若存在） |

> ⛔ **未读取模板，不得生成代码。** 模板的 props 接口、CSS 类名、层级结构只存在于模板源文件中。

### Step 3：制定改造计划

对模板中每个功能区域做决策：

| 决策 | 含义 |
|------|------|
| ✅ 保留 | PRD 中有，直接复用模板结构 |
| ✏️ 修改 | PRD 中有但内容不同，替换业务字段 |
| ➕ 新增 | PRD 有但模板没有，在副本中添加 |
| ❌ 删除 | PRD 没有，从副本中移除 |

### Step 4：生成代码

1. 完整复制模板到 `src/pages/{PageName}/`
2. 按 Step 3 计划逐区域修改副本（不动原模板）
3. 颜色/间距使用 `01-foundation/` 中定义的 Token / CSS 变量
4. 文案遵循 `03-design-db/copywriting.md`

### Step 5：交付前自检

| # | 检查项 | 要求 |
|---|--------|------|
| 1 | Step 2 模板已读取 | 本次 read_file 记录中包含所有涉及模板路径 |
| 2 | 页面嵌套在 AppLayout 内 | ✅ |
| 3 | 无硬编码颜色 | 全部使用 Token / CSS 变量 |
| 4 | 空值处理 | 统一显示 `–`（参考 copywriting.md） |
| 5 | 操作列固定 | 表格操作列 `fixed: 'right'` |
| 6 | 禁止自造组件库已有组件 | 直接使用组件库原生组件 |
| 7 | Modal 关闭销毁 | 使用组件库对应 API |

---

## ⛔ 模板保护规则

**`02-templates/` 目录下的所有文件为只读模板，任何情况下不得直接修改。**

- 开发新页面时，**完整复制**模板到 `src/pages/{PageName}/` 目录下
- 在副本上修改，原模板保持不变
- 只有用户明确说「更新模板」时，才允许修改 `02-templates/` 内的文件

---

## 三层架构总览

```
01-foundation/          ← 第一层：基础层（依赖声明 + 主题 Token）
02-templates/           ← 第二层：模板层（可复用页面模板）
03-design-db/           ← 第三层：设计规范层（颜色/字体/文案/原则）
src/pages/              ← 业务层（已交付的业务页，不在 skill 内）
```

---

> 💡 **完整设计规范见 `03-design-db/` 下各文件。**  
> 💡 **开箱配置向导见 `setup-guide.md`。**
